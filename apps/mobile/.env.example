# Hesably — local development environment template.
# Copy to `.env` (git-ignored) and fill in real values.
# The secret anon key is "publishable" (safe for clients); never put
# service-role keys or the Gemini API key here — those stay server-side.

SUPABASE_URL=
SUPABASE_ANON_KEY=